import { run } from './index.js';
import { expect, describe, it } from 'vitest';

describe('', () => {
  it('test', () => {
    // import run function and test it
    expect(run()).toBe('i run');
  });
});
